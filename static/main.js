"use strict";

//find DOM elements
const $recipeGrid = $("#recipe-grid");


async function start() {
    console.debug("start"); //in recipe.js
}
//once DOM is fully loaded, launch the app. 
    $(start);